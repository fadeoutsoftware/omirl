apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'lib/', include: ['*.jar'])
    compile project(':OmirlBusinessData')
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDir 'test'
        }
    }
}
